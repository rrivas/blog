.well
  %p#notice= notice
  #post_show_title
    %h2
      = @post.title
    %small.description= @post.description

  = markdown(@post.content)
  = Albino.colorize('puts "Hello World"', :ruby).html_safe

  %br
  - if user_signed_in?
    = link_to 'Edit', edit_post_path(@post)
    \|
  = link_to 'Back', posts_path
